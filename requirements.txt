# This file is kept for backwards compatibility
# Modern installations should use: uv pip install -e .
#
# See pyproject.toml for all dependencies
# See UV_GUIDE.md for usage instructions

# Install project with all dependencies:
-e .

# Or install with specific extras:
# -e .[training]  # Training dependencies
# -e .[gpu]       # GPU support
# -e .[dev]       # Development tools
# -e .[all]       # Everything

# Legacy pip install (slower):
#   pip install -r requirements.txt
#
# Modern uv install (faster):
#   uv pip install -e ".[all]"
